import React from 'react';

const ControlPanel = props => {
    return(
        <div>
            Control Panel
            <p>{props.algorithm}</p>
        </div>
    )
}

export default ControlPanel;